/** @description PayPal account details returned for a payment intent. */
export interface PaymentProduct840CustomerAccountForIntent {
  companyName?: string;
  firstName?: string;
  surname?: string;
  emailAddress?: string;
}
